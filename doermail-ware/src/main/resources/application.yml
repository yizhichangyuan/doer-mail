spring:
  datasource:
    username: root
    password: make1234
    # mysql连接需要开启批量插入，允许批量查询
    url: jdbc:mysql://10.211.55.4/gulimall_wms?rewriteBatchedStatements=true&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

  application:
    name: doermail-ware

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss


mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 11000

logging:
  level:
    com.lookstarry.doermail.ware.dao: debug
